services:
  - type: web
    name: instant-gold-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -u main.py
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
